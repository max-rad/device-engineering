mixin modeling(data)
  section.modeling
    .container
      .modeling__wrapper
        h2.title.title--h2.modeling__title!=data.title
        .modeling__content
          each item in data.content
            p!=item
          if (data.list)
            ul
              each item in data.list
                li!=item
